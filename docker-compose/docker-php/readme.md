## Docker PHP Compose

Php configuration via docker-compose.

Docker build:
```
docker-compose up --build
```

Docker run:

```
docker-compose up
```

## Documentation
https://docs.docker.com/compose/
